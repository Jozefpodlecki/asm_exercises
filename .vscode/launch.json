{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "PowerShell",
            "request": "launch",
            "name": "Run",
            "script": "${workspaceFolder}/run.ps1",
            "args": [
                "${file}"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}